FIE V9.2.1 GitHub Patch

This patch contains only files that are new or changed versus the uploaded V9.2 Kicker Intelligence repository.

- New files: 42
- Modified files: 109
- Deleted files: 0

Copy the contents of this folder into the ROOT of your GitHub repository and overwrite matching files.
Do not create an extra FIE-9.2.1-GitHub-Patch directory inside the repository.

Important: the 19 data/research/leagues/*/current/milestone5_current.json files must be overwritten. They are now lightweight manifests.
The new data/research/shared/current/ directory must also be added.
If Cloudflare deploys from dist/, replace/upload the changed dist/ contents as included here.
